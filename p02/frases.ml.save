let maximo = max_int;;
(*declara una variable maximo y la inicializa al maximo numero posible de un int*)
let minimo = min_int;;
(*declara una variable minimo y la inicializa al minimo numero posible de un int*)
minimo + maximo;;
(*suma minimo y maximo dando un resultado de -1 ya que es puede gardar un numero negativo mas que positivo*)
minimo + maximo + 1;;
(*da un resultado de 0*)
maximo + 1;;
(*int hace overflow ya que ya estaba en el valor maximo, por lo que el valor da la vuelta completa haciendo que tenga el mismo valor que minimo*)
minimo = maximo + 1;;
(*hace una comparacion de los dos valores dando un resultado de true ya que maximo se da la vuelta*)
2 * minimo;;
(*el resultado es 0 ya que recorre todos los valores positivos hasta llegar a 0*)
minimo - 1 = maximo;;
2 * maximo;;
(*da un resultado de -2 ya que el valor maximo positivo es 1 menos que el valor maximo negativo*)
let maximo = 1. /. 0.;;
(*se asigna el valor a infinito ya que esta intentado dividir por un numero muy pequeno*)
let minimo = -1.0 /. 0.;;
(*lo mismo que en el caso anterior pero con un numero muy pequeno*)
1. /. maximo;;
(*da 0 ya que un numero dividido entre 0, da un numero que se acerca a 0*)
1. /. minimo;;
(*lo mismo que el caso anterior pero en negativo*)
1. /. maximo = 1. /. minimo;;
(*devuelve true ya que esta comparando 0 y -0 lo cual viene a ser baiscamente lo mismo*)
0. /. 0.;;
maximo +. maximo;;
(*sumar infinito mas infinito te da igual a infinito*)
maximo -. maximo;;
(*restar infinito e infinito da un resultado que no se puede representar (not a number)*)
-. maximo = minimo;;
(*al poner el infinito a negativo y compararlo a menos infinito, da que los valores sean iguales*)
maximo + minimo;;
(*da un error ya que estas haciendo una suma de ints cuando los valores son floats*)
not (minimo < maximo);;
(*minimo es menor que falso, pero al tener el not, el valor del bool se invierto por lo que da false*)
let not = "no";;
(*asigna  el valor "no" a not*)
not (minimo < maximo);;
(*da un erorr ya que ahora not esta asignado a la variable que hemos declarado anteriormente*)
Stdlib.not (minimo < maximo);;
(*al especificar la libreria de la que viene la funcion, la linea funciona correctamente*)
let not = "si" in not ^ not;;
(*concatena la caneca de texto haciendo que tenga un valor de "sisi"*)
not;;
(*muestra el valor de not el cual es "no"*)
(function x -> 2 * x);;
(*es una funcion int a int que devuelve el doble del numero que se pasa*)
(function x -> 2 * x) (2 + 1);;
(*aplica la funcion a 2 + 1 que es 3 y devuelve 6*)
(function x -> 2 * x) 2 + 1;;
(*aplica la funcion a 2 y luego de suma 1 dando un resultado de 5*)
(function y -> 2 * y) ((function y -> 2 * y) 3);;
(*aplica basicamente la mismo funcion dos veces a 3 dando un resultado de 12*)
let doble = function z -> 2 * z;;
(*creamos una funcino doble que multiplica un int por 2*)
doble 2 + 1;;
(*aplica la funcion doble a 2 y luego le suma 1 dando 5*)
doble (doble 3);;
(*aplica la funcion doble dos veces a 3 dando un resultado de 12*)
doble doble 3;;
(*da error ya que esta intentando aplicadar la funcion doble a doble lo cual no es el tipo de parametro que quiere la funcion*)
abs (1 - 2);;
(*devuelve 1 ya que el resultado de 1 - 2 es -1 y lo vuelve positivo*)
abs 1;;
(*devuelve 1*)
abs -1;;
(*devuelve 1*)
(*correccion: da un error que ya coge primero el - y no el valor completo que es -1*)
let abs = function x -> if x >= 0. then x else -. x;;
abs 1;;
abs 1.5;;
Stdlib.abs 1;;
let suma = function (x,y) -> x + y;;
2 * suma (2,3) - suma (1,1);;
let sumac = function x -> (function y -> x + y);;
sumac 3;;
(sumac 3) 2;;
sumac 3 2;;
suma (3,2) = sumac 3 2;;
suma 3;;
let suma5 = sumac 5;;
suma5 10;;
let sumac x = function y -> x + y;;
sumac 3 2;;
let sumac x y = x + y;;
sumac 3 2;;
